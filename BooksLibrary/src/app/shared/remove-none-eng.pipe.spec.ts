import { RemoveNoneEngPipe } from './remove-none-eng.pipe';

describe('RemoveNoneEngPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveNoneEngPipe();
    expect(pipe).toBeTruthy();
  });
});
